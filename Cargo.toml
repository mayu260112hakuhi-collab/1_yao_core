[package]
name = "yao_core"
version = "0.1.0"
edition = "2021"

[dependencies]
# データの変換・処理系
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
urlencoding = "2.1"
regex = "1.12.3"

# 非同期処理（必要であれば）
tokio = { version = "1", features = ["full"] }